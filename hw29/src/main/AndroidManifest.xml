<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.alkarps.android.school2021.hw29">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidSchool2021">
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.add.AddActivity"
            android:windowSoftInputMode="adjustPan|adjustResize" />

        <provider
            android:name=".data.datastories.providers.DictionaryContentProvider"
            android:authorities="ru.alkarps.android.school2021.hw29"
            android:exported="true" />
    </application>
</manifest>